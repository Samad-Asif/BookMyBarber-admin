# Must end with /v1 — use ngrok HTTPS URL when backend is tunneled
VITE_API_URL=http://localhost:5000/v1
# VITE_API_URL=https://your-subdomain.ngrok-free.app/v1
# Set to false to silence dev console logs
VITE_DEBUG_LOGS=true
